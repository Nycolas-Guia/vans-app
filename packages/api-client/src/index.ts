// Client HTTP/WebSocket tipado que os apps motorista e responsavel usam
// para falar com apps/api.
//
// Intencionalmente vazio nesta fase (Fase 1 — setup do monorepo).
// Implementação real depende de @vans-app/domain-types (Fase 2) e dos
// endpoints do backend (Fase 3 em diante).

export {};
